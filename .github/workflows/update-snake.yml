name: Update contribution snake

on:
  schedule:
    - cron: "37 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: contribution-snake
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out profile
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate contribution snake
        uses: Platane/snk@v3
        with:
          github_user_name: Hughhhhcoder
          outputs: |
            assets/github-snake.svg?color_snake=%232F52FF&color_dots=%23F7F3EA,%23E0E3E8,%23B8C6B8,%232F52FF,%23E85D3A
            assets/github-snake-dark.svg?color_snake=%23B8FF5A&color_dots=%23171B1C,%23333B3D,%237D8B75,%23B8FF5A,%23FF9A76

      - name: Commit updated snake
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/github-snake.svg assets/github-snake-dark.svg
          if git diff --cached --quiet; then
            echo "No snake changes."
            exit 0
          fi
          git commit -m "chore: refresh contribution snake"
          git push
